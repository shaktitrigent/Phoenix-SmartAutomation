# Phoenix Configuration Example
# Copy this file and customize for your environment
# Or use environment variables instead

# Database Configuration
database:
  # For development (SQLite)
  url: "sqlite:///./phoenix.db"
  
  # For production (PostgreSQL)
  # url: "postgresql://user:password@localhost/phoenix"
  # pool_size: 10
  # max_overflow: 20

# Phoenix Intelligence Configuration
intelligence:
  # Versioned API base URL
  base_url: "http://localhost:8001/api/v1"
  timeout: 30
  retry_count: 3

# Cache Configuration
cache:
  # In-memory cache for MVP
  type: "memory"
  ttl: 3600  # seconds
  
  # For production (Redis)
  # type: "redis"
  # url: "redis://localhost:6379/0"

# Logging
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# Project Settings
project:
  default_project: "default"
  # Optional default application URL (used if --url not provided)
  application_url: "https://testautomationpractice.blogspot.com/"
  manual_output_dir: "./manual_tests"
  test_output_dir: "./test_results"
  report_output_dir: "./reports"
